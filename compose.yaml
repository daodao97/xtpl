services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
            target: final
        command: ["--bind", "0.0.0.0:8080"]
        ports:
            - "8080:8080"
